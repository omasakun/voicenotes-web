services:
  webapp:
    build:
      context: .
      dockerfile: Dockerfile.webapp
    ports:
      - "8080:80"
    restart: always
    env_file:
      - .env
    volumes:
      - ./data/data.sqlite:/app/prisma/data.sqlite
      - ./data/uploads:/app/uploads
